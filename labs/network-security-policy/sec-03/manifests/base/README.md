Nothing belongs in base/ for this lab — the entire starting scenario
(backend + both client Pods) is intentionally insecure (no NetworkPolicy at
all) and lives in manifests/broken/. This file is a placeholder so the
directory is not empty.
