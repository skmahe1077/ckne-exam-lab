# base/ — intentionally unused for SVC-07

This lab's only supporting object (the `web` Deployment) is the starting
scaffold the task is built around, so it lives in `manifests/broken/`
instead (see that directory's comment). Nothing is applied from `base/` by
`setup.sh` for this lab. This file exists only so the directory is not
empty, per the lab authoring convention.
