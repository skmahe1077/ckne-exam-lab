No always-correct supporting objects are needed for this lab — the only
objects are the `orders` Deployment + Service under `manifests/broken/`, and
this lab only reads (never modifies) the shared CoreDNS Pods in
`kube-system`.
