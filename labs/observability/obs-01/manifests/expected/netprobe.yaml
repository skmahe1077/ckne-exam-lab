# Reference fix: TARGET_URL corrected to the real Service name "backend".
# This is the only change required — the probe loop logic itself is already
# correct.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: netprobe
  namespace: ${NAMESPACE}
  labels:
    app: netprobe
    app.kubernetes.io/part-of: ckne-hands-on
    ckne.openai.com/lab-id: "${TASK_ID}"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: netprobe
  template:
    metadata:
      labels:
        app: netprobe
        app.kubernetes.io/part-of: ckne-hands-on
        ckne.openai.com/lab-id: "${TASK_ID}"
    spec:
      containers:
        - name: netprobe
          image: busybox:1.36
          env:
            - name: TARGET_URL
              value: "http://backend.${NAMESPACE}.svc.cluster.local"
          command: ["sh", "-c"]
          args:
            - |
              echo "netprobe starting, TARGET_URL=$TARGET_URL"
              if ! wget -q -T 3 -O /dev/null "$TARGET_URL"; then
                echo "ERROR: failed to reach $TARGET_URL (DNS/connection failure)"
                exit 1
              fi
              echo "OK: reached $TARGET_URL"
              while true; do
                if wget -q -T 3 -O /dev/null "$TARGET_URL"; then
                  echo "OK: reached $TARGET_URL"
                else
                  echo "ERROR: failed to reach $TARGET_URL (DNS/connection failure)"
                  exit 1
                fi
                sleep 5
              done
          resources:
            requests:
              cpu: 25m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 64Mi
